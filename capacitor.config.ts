import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Biblioteca_personal_digita_proyecto_final',
  webDir: 'www'
};

export default config;
